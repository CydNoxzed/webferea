client_config = dict(
    # address of the remote host, on which webferea is running
    REMOTE_HOST = 'myserver.de',
    # path on the remote server on which the liferea.db should be put
    REMOTE_DB_PATH = '/var/www/webferea/share/liferea.db',
    # temporary file
    TMP_PATH = '/tmp/liferea.db_tmp',
    # path to the local version of the liferea.db
    LOCAL_DB = "~/.local/share/liferea/liferea.db"
)